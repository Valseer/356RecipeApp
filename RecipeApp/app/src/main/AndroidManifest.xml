<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tyler_000.recipeapp" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Now You're Cooking With Scrum!"
        android:theme="@style/MyTheme" >
        <activity android:name=".RecipeSelector" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Step_Activity"
            android:label="Your Recipe" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.tyler_000.recipeapp.RecipeSelector" />
        </activity>
    </application>

</manifest>
